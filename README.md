
  # Create design prototype

  This is a code bundle for Create design prototype. The original project is available at https://www.figma.com/design/4PJUPZPaSS1DoH7UR9kNIR/Create-design-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  